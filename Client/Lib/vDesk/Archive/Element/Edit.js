/**
 * Namespace that contains plugins for editing Elements.
 * @namespace Edit
 * @memberOf vDesk.Archive.Element
 */
vDesk.Archive.Element.Edit = {
};

Object.defineProperty(vDesk.Archive.Element.Edit, "Registered", {
    enumerable: false,
   value:  function() {
       console.log(Object.values(vDesk.Archive.Element.Edit));
   }
});